{
  "name": "repo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build:packages": "pnpm --filter './packages/**' build",
    "build:serverless": "pnpm --filter @repo/serverless-function build",
    "start:serverless": "pnpm --filter @repo/serverless-function start"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.17.16",
    "@repo/tsconfig": "workspace:*",
    "rimraf": "6.0.1",
    "typescript": "^5.7.3"
  },
  "packageManager": "pnpm@10.4.1",
  "engines": {
    "node": ">18.18.x"
  }
}
